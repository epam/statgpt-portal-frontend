export enum SdmxDataFormat {
  CSV = 'csv',
  JSON = 'json',
  XML = 'xml',
}

export enum FileColumnsAttribute {
  ID = 'id',
  NAME = 'name',
  ID_NAME = 'both',
}

export enum DownloadType {
  FULL_DATASET = 'full',
  DATA_IN_TABLE = 'query',
}
