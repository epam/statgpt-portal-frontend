export enum CustomContentProperties {
  CHOICE = 'choice',
  COMPLETION = 'completion',
}

export enum CUSTOM_CHOICE_ID {
  COMPLETE = 'complete',
}

export enum STAGE_STATUS {
  COMPLETED = 'completed',
}
