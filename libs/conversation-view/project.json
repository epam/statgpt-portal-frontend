{
  "name": "conversation-view",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/conversation-view/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/libs/conversation-view"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "libs/conversation-view/**/*.{ts,tsx,js,jsx}",
          "libs/conversation-view/package.json"
        ]
      }
    },
    "publish": {
      "command": "node tools/publish-lib.mjs conversation-view --version={args.ver} --dry={args.dry} --tag={args.tag} --development={args.development}"
    }
  },
  "tags": ["type:lib", "scope:ui"]
}
